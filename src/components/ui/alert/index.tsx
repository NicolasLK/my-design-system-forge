import { AlertDescription, AlertIcon, AlertRoot, AlertTitle } from "./Alert"

export const Alert = {
    Root: AlertRoot,
    Icon: AlertIcon,
    Title: AlertTitle,
    Description: AlertDescription,
}